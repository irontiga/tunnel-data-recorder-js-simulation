// Simulate reading and converting sensor data.

function convertRawRangeSensorData (rawData) {
    return rawData
    // return (30 / (rawData / 205)) - 2.4
}

export default convertRawRangeSensorData
